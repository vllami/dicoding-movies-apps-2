<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.dicoding.capstoneakhir.watchlist">

    <dist:module
        dist:instant="false"
        dist:title="@string/title_watchlist">
        <dist:delivery>
            <dist:on-demand />
        </dist:delivery>

        <dist:fusing dist:include="true" />
    </dist:module>

    <application tools:ignore="AllowBackup">
        <activity
            android:name="com.dicoding.capstoneakhir.watchlist.ui.WatchlistActivity"
            android:parentActivityName="com.dicoding.capstoneakhir.ui.main.MainActivity"
            android:theme="@style/Theme.CapstoneAkhir.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />

                <data
                    android:host="watchlist"
                    android:scheme="capstoneakhir" />
            </intent-filter>
        </activity>
    </application>
</manifest>